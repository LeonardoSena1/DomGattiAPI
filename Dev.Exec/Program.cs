using Dev.Exec.RequestAPI;

class Program
{
    static void Main(string[] args)
    {
        Product.Exec();
        //try
        //{
        //    if (args.Length > 0)
        //        switch (args.First())
        //        {
        //            case "Product":
        //                Product.Exec();
        //                break;
        //            default:
        //                throw new Exception("Insira uma chave valida.");
        //        }
        //    else
        //        throw new Exception("Nenhuma chave inserida.");
        //}
        //catch (Exception ex)
        //{

        //}
    }
}